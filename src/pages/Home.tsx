import { useState } from "react";
import { AudioTrack } from "../components/AudioTrack";

export const HomePage = (): JSX.Element => {
  return (
    <div>
      <img src="/images/logo_full.png" alt="logo" className="w-1/2 mx-auto" />
    </div>
  );
};
